galaxy_info:
  author: Codey Oxley
  description: Multi-distro installation and configuration for pam_ssh_agent_auth
  license: license (WTFPL)
  min_ansible_version: 2.0
  platforms:
  - name: Ubuntu
    versions:
     - all
  - name: Archlinux
  galaxy_tags:
    - pam
    - ssh-agent
    - ssh
    - authentication
    - pam_ssh_agent_auth
    - Archlinux
    - Ubuntu
dependencies:
  - { role: coxley.packer, when: ansible_os_family == 'Archlinux'}
